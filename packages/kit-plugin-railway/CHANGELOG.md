# Changelog

## 0.1.2

- Corrected the README usage example to use the available `kit add railway/deploy` CLI command.
- Clarified that the adapter returns Railway configuration to kit's provisioning flow.
- Previous package manifest version: `0.1.1` (verified in git history).
