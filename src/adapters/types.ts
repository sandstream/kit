/**
 * Core types for kit service provisioning adapters.
 *
 * These types are published as a standalone package at `sandstream-kit-adapter-sdk`
 * so third-party developers can build adapters without depending on the full kit codebase.
 */
export type {
  ProvisionResult,
  AdapterContext,
  ServiceAdapter,
  AdapterRegistry,
} from "sandstream-kit-adapter-sdk";
