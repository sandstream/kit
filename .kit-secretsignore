# kit secrets allowlist — accepted historical findings, one per line:
#
#   <commit>:<file>:<detector>        (file may be * when trufflehog reports no path)
#
# An entry names ONE commit, so it can never wave through a future occurrence of the
# same string, and a VERIFIED-LIVE finding is never ignorable regardless of what is
# listed here. Everything else — placeholder credentials, unreachable hosts, doc sample
# tokens — is classified automatically and needs no entry (see isExampleCredential).
#
# Add an entry only after reading the finding and confirming it names nothing real.
# The point of the file is that the secrets check can reach green, so a NEW finding
# stands out instead of being one more line in a warning nobody reads.
#
# d9f55af — the secrets classifier's own negative fixtures. They must look live
# (routable host, high-entropy secret) to prove the classifier does NOT wave such a
# credential through. They were literals in that one commit; 1648ffa replaced them with
# run-time derived values, so nothing new accumulates. Nothing to rotate: the hosts
# (db.acme-prod.io, 10.4.2.9) are invented and the PAT was never issued.
d9f55afc26db3ed604946b44edf224c293405d3f:src/check-security.test.ts:Postgres
d9f55afc26db3ed604946b44edf224c293405d3f:src/check-security.test.ts:Github
d9f55afc26db3ed604946b44edf224c293405d3f:*:Postgres
